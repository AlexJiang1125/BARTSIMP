% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_test_data.R
\name{preprocess_test_data}
\alias{preprocess_test_data}
\title{Preprocess Test Data for BARTSIMP Spatial Prediction}
\usage{
preprocess_test_data(x_test, s_test = NULL)
}
\arguments{
\item{x_test}{Matrix or data frame of test covariates. Each row corresponds
to a test observation.}

\item{s_test}{Optional matrix or data frame containing spatial coordinates
for the test observations. Must contain exactly two columns representing
spatial coordinates (e.g., longitude and latitude).}
}
\value{
A list containing:

\describe{
\item{x_test}{
A data frame of processed test covariates.
}

\item{s_test}{
A data frame with columns \code{s1} and \code{s2} representing spatial coordinates.
}
}
}
\description{
Validates and formats test covariates and spatial coordinates for use in
BARTSIMP prediction routines. The function ensures that the covariate matrix
and spatial coordinate matrix have consistent dimensions and contain no
missing values.
}
\details{
This utility is primarily used internally by prediction functions to enforce
a consistent representation of test inputs.

The function performs the following checks:
\itemize{
\item Converts \code{x_test} and \code{s_test} to data frames.
\item Verifies that \code{s_test} has exactly two columns.
\item Ensures the number of rows in \code{x_test} and \code{s_test} match.
\item Ensures neither input contains missing values.
}

If \code{x_test} is empty (zero rows or zero columns), the function returns empty
objects for both covariates and spatial coordinates to allow downstream
prediction code to proceed safely.
}
\examples{
x_test <- data.frame(x1 = rnorm(5), x2 = runif(5))
s_test <- data.frame(lon = runif(5), lat = runif(5))

BARTSIMP:::preprocess_test_data(x_test, s_test)

}
\keyword{internal}
