# Write-Manuscript Decision Log

## Plan (Phase A)

- **Validation verdict:** CONDITIONAL GO (`validation_report.md`).
- **Primary advantage:** Admin-1 cov80 recentered toward nominal by the
  semi-dense depth-floor; primary paired Δcov80 = +0.0145 ± 0.0102 (24 reps,
  ≈1.4 MC SE — NOT individually significant, below the spec's +0.02 bar). The
  d₀ sweep is the headline calibration evidence (cov80 0.793 → broad plateau
  0.815–0.827 for d₀≥1, pixel cov80 flat ~0.87, cov95 ~0.94).
- **Comparators (every table):** proposed BARTSIMP-PG-SD; C1 BARTSIMP-PG (d₀=0,
  the arm to beat); C2 GLM; C3 Vanilla-BART; C4 GLMM-iCAR; C5 GLMM-SPDE;
  C6 disaggregation; C7 RF-GLS; C8 SPARforest. C1–C5 + proposed are FULL in the
  primary paired loop; C6–C8 are roster-context (compute-throttled, low-rep) per
  `comparator_inventory.md` (gate COMPLETE).
- **Production evaluations planned:** REUSE the validated 24-rep primary paired,
  16-rep d₀ sweep, 12-rep scale, 12-rep adversarial CSVs as the empirical core
  (a complete, coherent story). Launch a best-effort 60-rep production paired
  scale-up DETACHED (does NOT block the manuscript); fold in if it completes.
- **Theory:** Theorem 1 (Matérn/GP-only Admin-1 functional BvM) — PROVEN modulo
  re-regime fix (cite KvdVvZ Thm 5.4, not 5.3). Theorem 2 (combined BART+Matérn
  Admin-1 functional BvM, forward route) — CONJECTURED; the rigorous deliverable
  is the single-tree Gaussian sibling (s_a > p/2 representer decay confirmed,
  in-sample V_smp = σ²/π_a). BLY25/RoBART route ABANDONED (fatally holed,
  Mistake 13). Binomial-logit transfer + p=49 dimension wall OPEN.

## Orchestration note (execution model)

The write-manuscript SKILL dispatches literature-lead / paper-modeler /
paper-writer / paper-critic as separate Task sub-agents. In this phase-agent
context the Task dispatch primitive is not available; the orchestrator therefore
executes each sub-agent ROLE directly, following the substance of each SKILL
(briefings, production figures/audit, sectioned writing, adversarial critique).
The role separation and gate checks of the skill are preserved as sequential
self-contained passes, each logged below.

## Production scale-up

- Launched detached: `run_paired_dgp5.R --reps 60 --A 20 --tag production_scaleup`
  (Framework R, `nohup`, base_seed 90000 matched to validation so reps 1–24
  reproduce). Output → `phase3_write/scaleups/`. Poll, do NOT block.
- **Status at handoff:** still RUNNING at rep 9/60 after ~12 min (≈5 min/rep, as
  forecast). Did NOT complete within budget. DECISION: manuscript uses the
  validated 24-rep numbers; the ≥60–100-rep production run is stated honestly as
  the planned next step to resolve the primary contrast (Limitations (i),
  Future Work). Partial per-rep output preserved in `scaleups/`.

## Phase D — writing (paper-writer role)

- `manuscript.tex` written: 861 lines, JASA-style single article. Sections:
  Abstract + Keywords; 1 Introduction; 2 Related work; 3 Methods (Model, PG
  augmentation, semi-dense coarse-layer prior, estimand/summaries, sampler,
  assumptions); 4 Theory (Matérn-only BvM, combined BvM open prize, mechanism /
  cluster-floor / dimension-wall props); 5 Results (eval design, benchmark,
  primary paired, d₀ sweep headline, scale, adversarial, summary); 6 Discussion
  (anomalies A2/A3/A4 by ID, Limitations addressing A1+A2, Future work);
  7 Conclusion; Web Appendix.
- `references.bib`: 21 entries (≥10 required); all 21 cited (zero orphans after
  adding `dhs2014kenya` at the DHS motivation and `little2019statistical` at the
  missing-data-indicator contrast with RoBART).
- 5 table files written from the validated CSVs: `tab_primary.tex` (full roster
  GLM/Vanilla-BART/GLMM-iCAR/GLMM-SPDE/BARTSIMP-PG/BARTSIMP-PG-SD with the
  earned-vs-laundered calibration-ratio column), `tab_deltas.tex`,
  `tab_d0sweep.tex` (headline), `tab_scale.tex`, `tab_adversarial.tex`.
- 4 production PDF figures generated by `make_pdf_figures.R` (Framework R).

## Phase E — adversarial critique (paper-critic role)

- Honesty check PASS: abstract, primary subsection, and Limitations all state the
  primary Δcov80 = +0.0145 is below 2 MC SE / below the +0.02 bar (anomaly A1) and
  hand the claim to the corroborating sweeps; no overclaim.
- Theory-label check PASS: Thm 1 PROVEN-modulo (KvdVvZ 5.4 re-regime fix), Thm 2
  CONJECTURED, RoBART abandoned as a theory route (empirical comparator only),
  dimension wall s_a>p/2 fails at p=49 stated openly. Macros used: 4×PROVEN,
  5×CONJ, 2×PROVENMOD.
- Comparator-roster check PASS: full core roster in `tab_primary`; C6–C8 carried
  as reduced-rep roster context with the A6 caveat (Web Appendix).
- Anomaly coverage PASS: A1, A2 in Limitations by ID; A3, A4 in Discussion; A5 in
  scale subsection; A6 in comparator caveat.

## Phase F — compile + verification (gate)

- Toolchain: TeX Live 2026 `pdflatex` + `bibtex` (`plainnat.bst`, natbib round).
- Compile sequence `pdflatex → bibtex → pdflatex → pdflatex` exits 0; final PDF
  **17 pages**, no undefined references, no undefined citations, no "label(s) may
  have changed" warning remaining.
- Verification thresholds: manuscript 861 lines (>500 ✓); 4 PDF figures (≥2 ✓);
  21 bib entries (≥10 ✓), all cited; full comparator roster in tables ✓.
- DELIVERABLE COMPLETE.
